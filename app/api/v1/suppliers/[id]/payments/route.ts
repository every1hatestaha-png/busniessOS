import { z } from "zod";
import { ApiError, apiData, apiHandler, parseApiBody, requireApiContext } from "@/lib/server/api";
import { recordSupplierPayment, SupplierDomainError } from "@/lib/server/suppliers";
import { supplierPaymentSchema } from "@/lib/validation/supplier";
export const POST = apiHandler(async (request: Request, { params }: { params: Promise<{ id: string }> }) => { const context = await requireApiContext("payments.record"); const { id } = z.object({ id: z.uuid() }).parse(await params); const body = await request.clone().json().catch(() => ({})); const key = request.headers.get("Idempotency-Key"); const input = await parseApiBody(new Request(request.url, { method: "POST", headers: request.headers, body: JSON.stringify({ ...body, idempotencyKey: key ?? body.idempotencyKey }) }), supplierPaymentSchema); try { return apiData(await recordSupplierPayment({ ...context, userId: context.user.id }, id, input), 201); } catch (error) { if (error instanceof SupplierDomainError) throw new ApiError(422, "SUPPLIER_PAYMENT_ERROR", error.message); throw error; } });
