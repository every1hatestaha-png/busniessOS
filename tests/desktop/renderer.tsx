import React from "react";
import { createRoot } from "react-dom/client";
import { DesktopAccountMenu } from "../../components/layout/desktop-logout-button";

createRoot(document.getElementById("root")!).render(<DesktopAccountMenu />);
